<!-- ---
import Hero from "./Hero.tsx";
import RichText from "./RichText.astro";
import Pricing from "./Pricing.astro";
import Form from "./Form.astro";
import Gallery from "./Gallery.astro";

const { blocks } = Astro.props;
---

{
  blocks.map((block: any) => {
    if (block.collection === "block_hero") {
      return <Hero {...block} client:idle />;
    } else if (block.collection === "block_richtext") {
      return <RichText {...block} />;
    } else if (block.collection === "block_pricing") {
      return <Pricing {...block} />;
    } else if (block.collection === "block_form") {
      return <Form {...block} />;
    } else if (block.collection === "block_gallery") {
      return <Gallery {...block} />;
    }
    return null;
  })
} -->
